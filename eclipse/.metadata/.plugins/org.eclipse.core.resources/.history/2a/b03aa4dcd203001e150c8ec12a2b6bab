/*
 * Decompiled with CFR 0.150.
 */
package net.labymod.addons.flux.core.batching.buffer.graph;

import java.util.List;
import net.labymod.addons.flux.core.batching.buffer.graph.RenderGraph;
import net.labymod.addons.flux.core.batching.gl.FluxRenderType;

public class VanillaRenderGraph
implements RenderGraph<FluxRenderType> {
    private final List<FluxRenderType> renderTypes;

    public VanillaRenderGraph(List<FluxRenderType> renderTypes) {
        this.renderTypes = renderTypes;
    }

    @Override
    public int findIndex(FluxRenderType renderType) {
        return this.renderTypes.indexOf(renderType);
    }

    @Override
    public void addEntry(FluxRenderType renderType) {
        this.renderTypes.add(renderType);
    }

    @Override
    public FluxRenderType getEntry(int index) {
        return this.renderTypes.get(index);
    }

    @Override
    public void clear() {
        this.renderTypes.clear();
    }

    @Override
    public int size() {
        return this.renderTypes.size();
    }
}

